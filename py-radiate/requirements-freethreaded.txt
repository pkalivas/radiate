# Free-threaded (no-GIL) dependency set — used by `just sync` when the target
# interpreter is a free-threaded build (e.g. 3.13t / "3.13+freethreaded").
#
# PEP-508 markers can't distinguish a free-threaded interpreter (it reports the
# same `python_version` as the regular build), so packages that lack
# free-threaded wheels are simply omitted here rather than gated by marker:
#   jupyter -> pulls in `cffi`, which refuses to build on free-threaded 3.13
#   numba   -> no free-threaded support
#   torch   -> no stable free-threaded wheels yet
# Re-add any of these here once they ship free-threaded wheels.
maturin>=1.8.4,<2.0
numpy>=2.4.3
matplotlib>=3.8.0
pytest>=9.0.2
pytest-cov>=7.0.0
pytest-xdist==3.8.0
PyYaml>=6.0.3

polars>=1.38.1 ; python_version >= '3.12' and python_version < '3.14'
pandas>=2.1.0 ; python_version >= '3.12' and python_version < '3.14'
